{
  "name": "remote-storage-bridge",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "server",
    "dashboard"
  ],
  "scripts": {
    "build": "npm run build --workspace server && npm run build --workspace dashboard",
    "typecheck": "npm run typecheck --workspace server && npm run typecheck --workspace dashboard",
    "dev:server": "npm run dev --workspace server",
    "dev:dashboard": "npm run dev --workspace dashboard"
  }
}

